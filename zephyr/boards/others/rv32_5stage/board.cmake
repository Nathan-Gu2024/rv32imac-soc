# Custom RV32IMC 5-stage pipelined RISC-V CPU (Zynq-7020 FPGA soft-core)
# SPDX-License-Identifier: Apache-2.0

# No west flash runner configured yet - this board is currently flashed
# manually via xsdb (dow -data <bin> 0x0 -> fpga -f <bit> ->
# ps7_post_config), same as every other program in this project so far.
